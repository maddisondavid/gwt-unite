GWT UNITE NOTES:

This project assumes you have a User Library called GWT which contain the following GWT Jars from your GWT installation :

gwt-dev-windows.jar
gwt-servlet.jar
gwt-user.jar

Also note that this was built to work with GWT V1.7